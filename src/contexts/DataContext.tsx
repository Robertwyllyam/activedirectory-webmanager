import React, { createContext } from "react";

const DataContext = createContext({});

const DataContextProvider = () => {
  return <div>DataContextProvider</div>;
};

export default DataContextProvider;
